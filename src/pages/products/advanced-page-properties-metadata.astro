---
import SiteLayout from '../../layouts/SiteLayout.astro';
import { withBase } from '../../utils/paths';
---

<SiteLayout
  title="Advanced Page Properties & Metadata | Zeitdocs"
  description="Product placeholder for Advanced Page Properties & Metadata."
>
  <section class="hero">
    <div class="section-inner">
      <div class="product-hero-kicker">
        <img
          class="product-hero-logo"
          src={withBase('/assets/products/advanced-page-properties-metadata-logo.png')}
          alt=""
          width="389"
          height="402"
          decoding="async"
        />
        <span class="eyebrow">Product detail pending</span>
      </div>
      <h1>Advanced Page Properties & Metadata</h1>
      <p class="lead">
        This product area is reserved for structured Confluence metadata and improved
        page property workflows. Public documentation will be written after the
        product behavior is confirmed.
      </p>
    </div>
  </section>
</SiteLayout>

<style>
  .product-hero-kicker {
    display: flex;
    align-items: center;
    gap: 14px;
  }

  .product-hero-logo {
    display: block;
    width: 72px;
    height: 72px;
    object-fit: contain;
  }

  @media (max-width: 560px) {
    .product-hero-logo {
      width: 60px;
      height: 60px;
    }
  }
</style>
